const newDigit = (num) => {
    new_digit = num * 3; 
    return new_digit; 
}

const multiplier = (new_digit, mul) => {
    ans = new_digit * mul; 
    return ans; 
}

console.log(newDigit(2)); 
console.log(multiplier(new_digit, 1)); 